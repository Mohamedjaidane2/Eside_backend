spring:
  profiles:
    active: prod
  application:
    name: payment-microservice
  cloud:
    config:
      fail-fast: true
      retry:
        max-attempts: 20

logging.level:
  org.springframework.retry: trace
  com.netflix.discovery: trace
