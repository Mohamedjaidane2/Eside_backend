server:
  port: 8888

spring:
  profiles:
    active: native
  application:
    name: config-server
  cloud:
    config:
      :server:
        native:
      server:
        native:
          search-locations: classpath:/configurations
